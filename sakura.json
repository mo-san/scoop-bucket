{
    "version": "2.4.1",
    "description": "Sakura Editor: サクラエディタ",
    "homepage": "https://sakura-editor.github.io/",
    "license": "Zlib",
    "url": "https://github.com/sakura-editor/sakura/releases/download/v2.4.1/sakura-tag-v2.4.1-build2849-ee8234f-Win32-Release-Exe.zip",
    "hash": "4a906b211af88de6112e0cd636e867a2699202e36731140d9520b91b6649b319",
    "bin": "sakura.exe",
    "shortcuts": [
        [
            "sakura.exe",
            "サクラエディタ"
        ]
    ],
    "pre_install": [
        "$path = \"$persist_dir\"; if (!(Test-Path $path)) { New-Item $path -ItemType Directory | Out-Null }",
        "$path = \"$persist_dir\\sakura.ini\"; if (!(Test-Path $path)) { New-Item $path -ItemType File | Out-Null }"
    ],
    "persist": [
        "sakura.ini",
        "keyword",
        "Plugins"
    ],
    "checkver": {
        "url": "https://github.com/sakura-editor/sakura/releases/latest",
        "regex": "/v([\\d.]+)/(?<filename>sakura-.+-Release-Exe.zip)"
    },
    "autoupdate": {
        "url": "https://github.com/sakura-editor/sakura/releases/download/v${version}/${matchFilename}",
        "hash": {
            "url": "https://github.com/sakura-editor/sakura/releases/download/v${version}/${matchFilename}.md5"
        }
    }
}
